import 'package:flutter/material.dart';

class UpdateChildren extends StatelessWidget {
  const UpdateChildren({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("update");
  }
}
